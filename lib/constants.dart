import 'package:flutter/material.dart';

const kTaskCardBoxDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.only(
    topRight: Radius.circular(35.0),
    topLeft: Radius.circular(35.0),
  ),
);
